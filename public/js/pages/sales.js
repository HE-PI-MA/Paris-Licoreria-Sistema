/** Ventas U039: alta con pago, historial, detalle y anulación administrativa auditada. */
(() => {
  'use strict';
  const UI=window.ParisUI,D=UI.Decimal,admin=document.body.dataset.userRole==='ADMINISTRADOR';
  const field=(label,input)=>{const host=UI.element('div','app-field'),text=UI.element('label','app-label',label);text.htmlFor=input.id;host.append(text,input);return host;};
  const money=value=>UI.ValueFormat.number(value,{type:'price',locale:'es-BO',currency:'BOB'});
  class SaleForm{
    constructor({api,opener,onSaved}){this.api=api;this.onSaved=onSaved;this.lines=[];this.events=new AbortController();this.form=UI.element('form','app-form ops-sale-builder');this.form.id='sale-form-'+Date.now();
      const add=UI.element('section','ops-sale-add'),select=UI.element('select','app-input'),qty=UI.element('input','app-input'),button=UI.Button.create({label:'Agregar',icon:'plus',variant:'secondary'});
      select.id='sale-product';select.name='product';qty.id='sale-quantity';qty.type='number';qty.min='.001';qty.step='.001';qty.value='1';qty.inputMode='decimal';button.type='button';
      add.append(field('Producto o presentación',select),field('Cantidad',qty),button);this.form.append(add);this.select=select;this.qty=qty;
      this.linesHost=UI.element('div','ops-section');this.form.append(this.linesHost);this.total=UI.element('div','ops-total');this.form.append(this.total);
      const payments=UI.element('div','app-form-grid');this.cash=UI.element('input','app-input');this.cash.type='number';this.cash.min='0';this.cash.step='.01';this.cash.value='0';this.cash.id='sale-cash';this.cash.name='cash';
      this.qr=UI.element('input','app-input');this.qr.type='number';this.qr.min='0';this.qr.step='.01';this.qr.value='0';this.qr.id='sale-qr';this.qr.name='qr';
      this.receipt=UI.element('input','app-input');this.receipt.type='text';this.receipt.maxLength=255;this.receipt.id='sale-receipt';this.receipt.name='receipt';this.receipt.placeholder='Referencia o comprobante';
      payments.append(field('Pago en efectivo (Bs)',this.cash),field('Pago por QR (Bs)',this.qr),field('Comprobante QR',this.receipt));this.form.append(payments);
      let controller;this.modal=new UI.Modal({title:'Nueva venta',icon:'cart',size:'large',content:this.form,isDirty:()=>Boolean(this.lines.length)||controller?.isDirty(),onClose:()=>{controller?.destroy();this.enhanced?.destroy();this.events.abort();this.modal.destroy();}});
      const cancel=UI.Button.create({label:'Cancelar'}),save=UI.Button.create({label:'Registrar venta',variant:'primary',type:'submit'});save.setAttribute('form',this.form.id);cancel.addEventListener('click',()=>this.modal.requestClose(),{signal:this.modal.events.signal});this.modal.footer.append(cancel,save);
      button.addEventListener('click',()=>this.addLine(),{signal:this.events.signal});
      this.operation=api.operation('');controller=new UI.FormController({form:this.form,modal:this.modal,validate:()=>this.validate(),onSubmit:(_,opts)=>this.operation(this.payload(),opts),onSuccess:async result=>{this.lines=[];this.modal.close();await onSaved(result);}});this.controller=controller;
      this.loadProducts().then(()=>{if(!this.modal.destroyed)this.render();});this.modal.open(opener);
    }
    async loadProducts(){try{const rows=await this.api.request('/productos?term=');const empty=UI.element('option','','Selecciona un producto');empty.value='';this.select.append(empty);for(const row of rows){const o=UI.element('option','',`${row.product} — ${row.presentation} — ${money(row.price)} — disp. ${row.available}`);o.value=String(row.id);o.dataset.product=row.product;o.dataset.presentation=row.presentation;o.dataset.price=row.price;o.dataset.available=row.available;this.select.append(o);}this.enhanced=new UI.SearchSelect({select:this.select,placeholder:'Escribe para buscar producto…'});}catch(error){window.ParisModule.showMessage('error',error.userMessage||'No se pudieron cargar productos vendibles.');}}
    addLine(){const option=this.select.selectedOptions[0],quantity=this.qty.value;if(!option?.value){this.enhanced?.input.focus();return;}let q,a;try{q=D.units(quantity,3);a=D.units(option.dataset.available,3);}catch(_){q=0n;a=0n;}if(q<=0n||q>a){this.qty.setAttribute('aria-invalid','true');this.qty.focus();return;}this.qty.removeAttribute('aria-invalid');const id=Number(option.value),existing=this.lines.find(row=>row.presentationId===id);if(existing){const total=D.units(existing.quantity,3)+q;if(total>a){this.qty.setAttribute('aria-invalid','true');this.qty.focus();return;}existing.quantity=D.text(total,3);}else this.lines.push({presentationId:id,product:option.dataset.product,presentation:option.dataset.presentation,price:option.dataset.price,available:option.dataset.available,quantity:D.text(q,3)});this.enhanced?.setValue(null);this.qty.value='1';this.render();}
    totalValue(){let total=0n;for(const row of this.lines)total+=D.units(D.multiply(row.quantity,row.price,2),2);return D.text(total,2);}
    render(){this.linesHost.replaceChildren();if(!this.lines.length)this.linesHost.append(UI.element('p','ops-empty','Agrega los productos de la venta.'));else{const table=UI.element('table','ops-lines'),head=document.createElement('thead'),tr=document.createElement('tr');for(const h of ['Producto','Cantidad','Precio','Subtotal',''])tr.append(UI.element('th','',h));head.append(tr);const body=document.createElement('tbody');this.lines.forEach((row,index)=>{const line=document.createElement('tr');line.append(UI.element('td','',row.product+' — '+row.presentation),UI.element('td','',row.quantity),UI.element('td','',money(row.price)),UI.element('td','',money(D.multiply(row.quantity,row.price,2))));const action=UI.element('td'),remove=UI.Button.create({label:'Quitar',icon:'trash',variant:'danger',iconOnly:true});remove.addEventListener('click',()=>{this.lines.splice(index,1);this.render();},{once:true});action.append(remove);line.append(action);body.append(line);});table.append(head,body);this.linesHost.append(table);}this.total.replaceChildren(UI.element('span','','Total'),UI.element('strong','',money(this.totalValue())));}
    validate(){const errors={};if(!this.lines.length)errors.cash='Agrega al menos un producto antes de registrar.';let cash=0n,qr=0n,total=0n;try{cash=D.units(this.cash.value||'0',2);qr=D.units(this.qr.value||'0',2);total=D.units(this.totalValue(),2);}catch(_){errors.cash='Revisa los importes del pago.';}if(cash<0n||qr<0n||cash+qr!==total)errors.cash='Efectivo + QR debe ser exactamente igual al total de la venta.';if(qr>0n&&!this.receipt.value.trim())errors.receipt='Escribe el comprobante o referencia del QR.';return errors;}
    payload(){const payments=[];if(D.units(this.cash.value||'0',2)>0n)payments.push({method:'EFECTIVO',amount:D.text(D.units(this.cash.value,2),2),receipt:''});if(D.units(this.qr.value||'0',2)>0n)payments.push({method:'QR',amount:D.text(D.units(this.qr.value,2),2),receipt:this.receipt.value.trim()});return{details:this.lines.map(row=>({presentationId:row.presentationId,quantity:row.quantity})),payments};}
  }
  class SalesPage{
    constructor(host){this.api=new UI.CatalogApi('/api/ventas');this.events=new AbortController();this.notifications=UI.NotificationCenter.shared();this.dialogs=new Set();
      this.table=new UI.DataTable({container:host,caption:'Ventas',mode:'scroll',numbered:true,pageSize:50,load:p=>this.api.list(p),sort:{key:'date',direction:'desc'},actionDisplay:'menu',columns:[{key:'date',label:'Fecha',sortable:true},{key:'cash',label:'Caja',priority:1},{key:'user',label:'Registrado por',sortable:true,priority:2},{key:'total',label:'Total',type:'price',sortable:true},{key:'state',label:'Estado',type:'state',sortable:true,states:{VIGENTE:{label:'Vigente',tone:'success'},ANULADA:{label:'Anulada',tone:'inactive'}}}],actions:[{id:'detail',label:'Ver detalle',icon:'info'},{id:'cancel',label:'Anular venta',icon:'warning',tone:'warning',visible:r=>admin&&r.state==='VIGENTE'}],onAction:item=>this.action(item)});
      this.filters=new UI.FilterBar({container:document.querySelector('[data-module-region="controls"]'),searchInput:document.getElementById('module-search'),mode:'inline',fields:[{name:'state',label:'Estado',type:'select',control:document.getElementById('module-state'),emptyLabel:'Todos los estados',options:[{value:'VIGENTE',label:'Vigentes'},{value:'ANULADA',label:'Anuladas'}]}],onChange:q=>this.table.setQuery(q)});
      document.querySelector('[data-module-primary]').addEventListener('click',e=>this.openForm(e.currentTarget),{signal:this.events.signal});window.addEventListener('pagehide',()=>this.destroy(),{once:true,signal:this.events.signal});}
    track(modal){this.dialogs.add(modal);const close=modal.onClose;modal.onClose=v=>{close(v);this.dialogs.delete(modal);};}
    openForm(opener){const form=new SaleForm({api:this.api,opener,onSaved:async result=>{this.notifications.show('success','Venta N.º '+result.id+' registrada correctamente.');await this.table.refresh();}});this.track(form.modal);}
    async action({action,record,button}){try{const row=await this.api.detail(record.id);if(action==='detail')this.detail(row,button);else this.cancel(row,button);}catch(error){this.notifications.show('error',error.userMessage||'No se pudo completar la operación.');}}
    detail(record,opener){const content=UI.element('div','ops-stack'),details=new UI.RecordDetails({record,fields:[{key:'date',label:'Fecha'},{key:'cash',label:'Caja'},{key:'user',label:'Registrado por'},{key:'state',label:'Estado',type:'state'},{key:'total',label:'Total',type:'price'},{key:'cancelledAt',label:'Anulada el',empty:'—'},{key:'cancelledBy',label:'Anulada por',empty:'—'},{key:'cancellationReason',label:'Motivo de anulación',wide:true,empty:'—'}]});content.append(details.element);const section=UI.element('section','ops-section'),table=UI.element('table','ops-lines');table.innerHTML='<thead><tr><th>Producto</th><th>Cantidad</th><th>Precio</th><th>Subtotal</th></tr></thead>';const body=document.createElement('tbody');for(const line of record.lines){const tr=document.createElement('tr');tr.append(UI.element('td','',line.product+' — '+line.presentation),UI.element('td','',line.quantity),UI.element('td','',money(line.price)),UI.element('td','',money(line.subtotal)));body.append(tr);}table.append(body);section.append(table);content.append(section);const pay=UI.element('section','ops-section');for(const p of record.payments)pay.append(UI.element('p','',`${p.method}: ${money(p.amount)}${p.refundReference?' · devolución '+p.refundReference:''}`));content.append(pay);const modal=new UI.Modal({title:'Venta N.º '+record.id,icon:'cart',size:'large',content,onClose:()=>{modal.destroy();this.dialogs.delete(modal);}}),close=UI.Button.create({label:'Cerrar'});close.addEventListener('click',()=>modal.requestClose(),{signal:modal.events.signal});modal.footer.append(close);this.dialogs.add(modal);modal.open(opener);}
    cancel(record,opener){const form=UI.element('form','app-form');form.id='cancel-sale-'+record.id;const reason=UI.element('textarea','app-input'),reference=UI.element('input','app-input');reason.id='cancel-reason';reason.name='reason';reason.required=true;reason.maxLength=250;reference.id='cancel-ref';reference.name='qrRefundReference';reference.maxLength=255;const hasQr=record.payments.some(p=>p.method==='QR');if(hasQr)reference.required=true;form.append(field('Motivo de la anulación',reason));if(hasQr)form.append(field('Referencia/comprobante de devolución QR',reference));let controller;const modal=new UI.Modal({title:'Anular venta N.º '+record.id,icon:'warning',size:'medium',content:form,isDirty:()=>controller?.isDirty(),onClose:()=>{controller?.destroy();modal.destroy();this.dialogs.delete(modal);}}),back=UI.Button.create({label:'Cancelar'}),confirm=UI.Button.create({label:'Anular venta',variant:'danger',type:'submit'});confirm.setAttribute('form',form.id);back.addEventListener('click',()=>modal.requestClose(),{signal:modal.events.signal});modal.footer.append(back,confirm);controller=new UI.FormController({form,modal,onSubmit:values=>this.api.request('/'+record.id+'/anular',{body:{reason:values.reason,qrRefundReference:values.qrRefundReference||''},key:UI.CatalogApi.newKey()}),onSuccess:async()=>{modal.close();this.notifications.show('success','Venta anulada y devolución registrada.');await this.table.refresh();}});this.dialogs.add(modal);modal.open(opener);}
    destroy(){if(this.destroyed)return;this.destroyed=true;this.events.abort();this.filters.destroy();this.table.destroy();this.notifications.destroy();for(const m of this.dialogs)m.destroy();this.dialogs.clear();}
  }
  const host=document.getElementById('sales-table');if(host)new SalesPage(host);
})();
